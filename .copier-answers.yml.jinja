# This file records the answers used to generate this project from the Project Keel
# template, so `copier update` can pull future template improvements. It is managed
# by Copier — do NOT edit by hand (changes here are overwritten on update).
{{ _copier_answers|to_nice_yaml }}
